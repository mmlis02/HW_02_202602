"""routing — Fase 2

Cliente OSRM local (Docker). Snapping a la red, matrices de tiempo por perfil (car/bike/foot) via /table en bloques, cache en data/cache/. Estados de ruteo explicitos; nunca distancia haversine como sustituto.

No implementado todavia (Fase 0 = solo andamiaje).
"""
